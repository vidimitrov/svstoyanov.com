import styled from 'styled-components';
import { colors } from '../../styles';

const TsulaText = styled.span`
  font-family: 'Space Mono';
  font-size: 0.85em;
	letter-spacing: 0.2em;
	color: ${colors.primary};
`;

export default TsulaText;
