import styled from 'styled-components';

const ImageStep = styled.img`
  width: 100%;
  height: 100%;
  border: 3px solid #02a0a7;
  border-radius: 3px;

  @media (max-width: 420px) {
    width: 150px;
  }
`;

export default ImageStep;
