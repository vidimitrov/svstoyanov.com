const colors = {
  white: '#fff',
  black: '#000',
  gray: 'gray',
};
const wrapper = {
  wrapper: {
    height: '100%',
  },
};

const shadow = {
  boxShadow: 'inset 0px 200px 200px rgba(0, 0, 0, 0.9)',
  width: '100%',
  height: '100%',
  position: 'absolute',
};

export {
  colors,
  wrapper,
  shadow,
};
